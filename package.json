{
  "name": "remote-exec-js",
  "version": "0.0.0",
  "description": "```javascript var remoteExec = require('remote-exec'),     r = new remoteExec();",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "chalk": "^0.5.1"
  }
}
